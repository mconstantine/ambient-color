log_level = "off"
enable_esc_key = true

[modules]
left = ["SystemInfo", "Updates"]
center = ["Workspaces", "WindowTitle"]
right = ["Clock", "Settings"]

[updates]
check_cmd = "checkupdates; yay -Qua"
update_cmd = 'foot -e bash -c "yay; echo Done - Press enter to exit; read" &'

[appearance]
font_name = "NotoSansM Nerd Font Mono"
text_color = "{{ primary.w900.fg }}"

[appearance.success_color]
base = "#05df72"
text = "#032e15"

[appearance.primary_color]
base = "{{ opposite.w400.bg }}"
text = "{{ opposite.w400.fg }}"

[appearance.danger_color]
base = "#ff6266"
text = "#440607"

[appearance.background_color]
base = "{{ primary.w900.bg }}"
text = "{{ primary.w900.fg }}"

[appearance.secondary_color]
base = "{{ secondary.w400.bg }}"
text = "{{ secondary.w400.fg }}"
