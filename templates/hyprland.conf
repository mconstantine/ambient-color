general {
    col.active_border = rgb({{ primary_500 | no_hashtag }})
    col.inactive_border = rgb({{ primary_950 | no_hashtag }})
}

misc {
    background_color = 0x{{ primary_950 | no_hashtag }}
}
