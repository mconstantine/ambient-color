<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="com.google.android.wearable.permission.RECEIVE_COMPLICATION_DATA" />

    <uses-feature android:name="android.hardware.type.watch" android:version="1" />
    <!-- Note: hasCode is required to be false for Watch Face Format -->
    <application
        android:hasCode="false"
        android:label="@string/watch_face_name">
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <property
            android:name="com.google.wear.watchface.format.version"
            android:value="4" />

        <!-- Optionally add details about the publisher. -->
        <!--
           <property
          android:name="com.google.wear.watchface.format.publisher"
          android:value="YourPublisherNameHere" />
        -->
    </application>

</manifest>